# ── JWT ──
# QUAN TRỌNG: Phải khớp với key ký JWT trong Identity Service (Spring Boot)
JWT_SECRET_KEY=your-shared-secret-key-must-match-spring-boot
JWT_ALGORITHM=HS256

# ── Service ──
SERVICE_PORT=8000
DEBUG=false

# ── ML ──
CSV_FILE=app/data/comments_dataset.csv
MODEL_PATH=app/ml/saved_model
